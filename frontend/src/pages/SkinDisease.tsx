import { ComingSoonState } from '@/components/ui/ComingSoonState';

export default function SkinDisease() {
  return (
    <ComingSoonState
      title="Skin Disease"
      description="This dermatology workspace is reserved for image classification and lesion triage in a future phase."
      note="The frontend route and layout are ready; the model integration is intentionally deferred."
    />
  );
}
