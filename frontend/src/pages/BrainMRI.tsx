import { ComingSoonState } from '@/components/ui/ComingSoonState';

export default function BrainMRI() {
  return (
    <ComingSoonState
      title="Brain MRI"
      description="A dedicated neurology module will live here once the MRI model and data pipeline are ready."
      note="Reserved for future segmentation, triage, and review workflows."
    />
  );
}
